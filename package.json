{
    "name": "expo-supabase-monorepo",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "yarn workspace mobile start",
        "build": "echo 'Use Expo CLI to build your app (e.g., expo build:android)'",
        "clean": "rimraf node_modules && rimraf apps/**/node_modules && rimraf packages/**/node_modules && yarn install"
    },
    "devDependencies": {
        "rimraf": "^5.0.0"
    },
    "dependencies": {
        "react-native-polyfill-globals": "^3.1.0"
    }
}
